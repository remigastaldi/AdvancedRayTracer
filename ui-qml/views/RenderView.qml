import AdvancedRayTracer 1.0
import "../styles"

import QtQuick 2.9
import QtQuick.Window 2.2

CustomRenderItem {
    anchors.fill: parent
    objectName: "renderPainter"
    // Keys.onPressed: keyPressedEvent(event.key)
}